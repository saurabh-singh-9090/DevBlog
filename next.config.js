/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'i.pravatar.cc', 'example.com', 'images.pexels.com'],
  },
};

module.exports = nextConfig; 